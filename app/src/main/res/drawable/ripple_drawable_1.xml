<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#FF00FF00">
    <!-- 指定id 再显示的时候 默认不显示该drawable 而是再点击的时候显示, 好像不好使，点击的时候 没有展示-->
    <item android:id="@android:id/mask" android:drawable="@mipmap/icon"/>
</ripple>